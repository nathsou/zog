
let tuple: (num => num, num[], { a: str, b: str }) = (n => 3 * n, [1, 2, 3], { a: "hello", b: "zog" })
tuple->print

let sum = (elems: num[]): num => {
    mut total = 0
    
    for n in elems {
        total += n
    }
    
    total
}
