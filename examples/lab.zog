import "../std/foundation.zog" { print }

let showNum: num => str = raw { n => String(n) }

trait Show<t> {
    fun show(self: t): str
}

impl trait Show<a> {
    fun show(n) {
        showNum(n) 
    }
}
