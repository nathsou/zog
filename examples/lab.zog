
let range = iterator (start, end, step) -> {
    mut i = start
    
    while i < end {
        yield i
        i += step
    }
}

let fact = n -> if n == 0 { 1 } else { n * fact(n - 1) }

let f = x -> 3 * x
let yo = if f(7 + 1) > 21 { 1 } else { 2 }
let id = x -> x

for n in range(0, 10, 1) {
    print(n)
}

let tuple = ("zog", 3 * 7, !(7 > 11))

print(tuple)

print({
    let a = { let a = 1; let b = 2; a + b }
    let b = { let a = 2; let b = a; let c = b; a * b * c }
    a * b
})
