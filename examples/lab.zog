import "../std/foundation.zog" { print }

let showNum: num => str = raw { n => String(n) }

trait Show {
    fun show(self): str
}

impl Show for num {
    fun show(self) {
       showNum(self) 
    }
}
